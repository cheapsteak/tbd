# Implementation plans

Implementation plans live in this directory but are **not** tracked by git
(`docs/superpowers/plans/` is gitignored, with this `CLAUDE.md` as the sole
exception). Still write plans here — local-only is intentional, the directory
is the right home for them.
