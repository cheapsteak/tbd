[
  "CREATE TABLE IF NOT EXISTS t (a INTEGER, b INTEGER);",
  "CREATE TRIGGER IF NOT EXISTS t_ai AFTER INSERT ON t\nBEGIN\n  UPDATE t SET a = 1;\n  UPDATE t SET b = 2;\nEND;",
  "UPDATE t SET a = 3;"
]
